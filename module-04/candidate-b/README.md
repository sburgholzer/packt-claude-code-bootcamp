# Notes API

A simple FastAPI-based Notes API with SQLite persistence.

## Setup

```bash
pip install fastapi uvicorn pydantic
```

## Run

```bash
uvicorn main:app --reload
```

API runs on http://localhost:8000 with interactive docs at http://localhost:8000/docs.
