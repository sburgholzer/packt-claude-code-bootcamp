# Notes API (Track A — Python / FastAPI / SQLite)

## Run

```bash
pip install fastapi uvicorn
uvicorn main:app --reload
```

API available at `http://localhost:8000`. Schema initialised automatically on first run; data persisted in `notes.db`.
